$('#search_results option').remove();
<%- @spots.each do |spot| -%>
$('#search_results').append($('<option>').attr({value: <%= spot.id %>, 'mtp-latitude': <%= spot.latitude %>, 'mtp-longitude': <%= spot.longitude %>}).text("<%= escape_javascript spot.name %>"));
<%- end -%>
